from setuptools import setup, find_packages

setup(
    name='project',
    version='',
    packages=find_packages(exclude=("tests",)),
    url='',
    license='',
    author='f4nni',
    author_email='',
    description=''
)
